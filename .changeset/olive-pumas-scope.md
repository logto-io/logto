---
"@logto/console": patch
---

fix webhook test results being displayed on other webhooks' details pages

Test results are now persisted per webhook, so a test result from one webhook is no longer shown when viewing another webhook.
