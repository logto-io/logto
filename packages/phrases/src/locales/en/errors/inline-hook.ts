const inline_hook = {
  general: 'An error occurred while running the inline hook. Please try again later.',
  sign_in_identifier_changed:
    'The inline hook cannot change the sign-in identifier used for this verification.',
};

export default Object.freeze(inline_hook);
